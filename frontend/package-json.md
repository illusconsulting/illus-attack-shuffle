```json
{
  "name": "attack-shuffle-frontend",
  "version": "1.0.0",
  "description": "Frontend for the Attack Shuffle project using Vue.js",
  "author": "Your Name",
  "license": "MIT",
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "axios": "^1.4.0",            // HTTP client for API requests
    "core-js": "^3.30.0",         // Polyfills for modern JavaScript features
    "vue": "^3.3.0",              // Vue.js framework
    "vue-router": "^4.2.0",       // Vue Router for navigation
    "vuex": "^4.1.0"              // Vuex for state management
  },
  "devDependencies": {
    "@vue/cli-service": "^5.0.0", // CLI for Vue project management
    "@vue/cli-plugin-babel": "^5.0.0", // Babel plugin for Vue CLI
    "@vue/cli-plugin-eslint": "^5.0.0", // ESLint plugin for Vue CLI
    "eslint": "^8.50.0",           // Linter for JavaScript
    "eslint-plugin-vue": "^9.14.0" // ESLint plugin for Vue-specific linting
  },
  "engines": {
    "node": ">=16.0.0",           // Recommended Node.js version
    "npm": ">=8.0.0"              // Recommended npm version
  }
}
```